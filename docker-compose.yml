version: '3.8'

services:
  web:
    build: .
    container_name: ffwhbg-kalender
    ports:
      - "8112:80"
    volumes:
      - .:/var/www/html
      - /var/www/html/node_modules
    restart: always
